[package]
name = "lvgl-sys"
version = "0.1.0"
edition = "2021"

[build-dependencies]
cc = { version = "1", features = ["parallel"] }
pkg-config = "0.3"
bindgen = "0.72.1"

[dependencies]
cty = "0.2.2"

[features]
sdl = []
